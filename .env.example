# HOORAIN admin local development. Copy to .env and adjust as needed.
# Never commit real secrets. Vite only exposes VITE_* to the browser.

VITE_API_URL=http://localhost:8000

# Optional. Order WhatsApp links fall back to 923000000000 if unset.
VITE_WHATSAPP_NUMBER=
